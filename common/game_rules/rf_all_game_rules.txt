culture_merging = {
    default = culture_merging_disabled

    culture_merging_disabled = {    }

    culture_merging_enabled = {
        flag = culture_merging_enabled
    }
}

rf_annex_decentralized_annexation = {
    default = annexation_disabled

    annexation_unlimited = {
        flag = rf_annex_decentralized_unlimited
    }

    annexation_limited = {
        flag = rf_annex_decentralized_limited
    }

    annexation_disabled = {
    }
}

ai_improvement = {
    default = ai_improvement_lite

    ai_improvement_lite = {
        flag = ai_improvement_lite
    }

    ai_improvement_on = {
        flag = ai_improvement_on
    }

    ai_improvement_disabled = {
    }
}

dynamic_country_ai_creation = {
    default = dynamic_country_ai_creation_gameplay

    dynamic_country_ai_creation_off = {
    }

    dynamic_country_ai_creation_roleplay = {
        flag = dynamic_country_ai_creation_roleplay
    }

    dynamic_country_ai_creation_gameplay = {
        flag = dynamic_country_ai_creation_gameplay
    }
}

rf_colony_grant_claim = {
    default = colony_grant_claim_enabled

    colony_grant_claim_enabled = {
        flag = rf_colony_grant_claim_enabled
    }

    colony_grant_claim_disabled = {
    }
}

rf_stronger_rebellions = {
    default = rf_stronger_rebellions_enabled

    rf_stronger_rebellions_enabled = {
        flag = rf_stronger_rebellions_enabled
    }

    rf_stronger_rebellions_disabled = {
    }
}

rf_forced_colonization = {
    default = rf_forced_colonization_enabled

    rf_forced_colonization_enabled = {
        flag = rf_forced_colonization_enabled
    }

    rf_forced_colonization_disabled = {
    }
}

rf_personal_union = {
    default = rf_personal_union_enabled

    rf_personal_union_enabled = {
        flag = rf_personal_union_enabled
    }

    rf_personal_union_disabled = {
    }
}
