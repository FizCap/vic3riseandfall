et_reserve_income_ratio = {
    if = {
        limit = { income > expenses }
        value = gold_reserves_limit
        subtract = expenses
        divide = income
    }
    else = {
        value = 1000
    }
}


et_autobuild_reserve_limit = {
    # Reserve limit for autobuilding: 45% of gold_reserves_limit
    value = gold_reserves_limit
    multiply = 0.45
}