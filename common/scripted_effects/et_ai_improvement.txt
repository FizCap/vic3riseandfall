et_ai_improvement_effect_great_power = {
    if = {
        limit = {
            has_game_rule = ai_improvement_enabled
            is_ai = yes
            country_rank = rank_value:great_power
        }
        if = {
            limit = { weekly_net_fixed_income < 10000 }
            set_government_wage_level = very_low
            set_military_wage_level = very_low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else_if = {
            limit = { weekly_net_fixed_income < 30000 }
            set_government_wage_level = low
            set_military_wage_level = low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else = {
            set_government_wage_level = medium
            set_military_wage_level = medium
            set_variable = { name = et_budget_cooldown days = 30 }
        }

        # Bureaucracy modifier logic always runs
        if = {
            limit = {
                bureaucracy < -10
                NOT = { has_modifier = et_negative_bureaucracy }
            }
            add_modifier = { name = et_negative_bureaucracy days = 365 hidden = yes }
        }
    }
}

et_ai_improvement_effect_major_power = {
    if = {
        limit = {
            has_game_rule = ai_improvement_enabled
            is_ai = yes
            country_rank = rank_value:major_power
        }
        if = {
            limit = { weekly_net_fixed_income < 4000 }
            set_government_wage_level = very_low
            set_military_wage_level = very_low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else_if = {
            limit = { weekly_net_fixed_income < 12000 }
            set_government_wage_level = low
            set_military_wage_level = low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else = {
            set_government_wage_level = medium
            set_military_wage_level = medium
            set_variable = { name = et_budget_cooldown days = 30 }
        }

        # Bureaucracy modifier logic always runs
        if = {
            limit = {
                bureaucracy < -10
                NOT = { has_modifier = et_negative_bureaucracy }
            }
            add_modifier = { name = et_negative_bureaucracy days = 365 hidden = yes }
        }
    }
}

et_ai_improvement_effect_minor_power = {
    if = {
        limit = {
            has_game_rule = ai_improvement_enabled
            is_ai = yes
            country_rank < rank_value:minor_power
        }
        if = {
            limit = { weekly_net_fixed_income < 1000 }
            set_government_wage_level = very_low
            set_military_wage_level = very_low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else_if = {
            limit = { weekly_net_fixed_income < 4000 }
            set_government_wage_level = low
            set_military_wage_level = low
            set_variable = { name = et_budget_cooldown days = 30 }
        }
        else = {
            set_government_wage_level = medium
            set_military_wage_level = medium
            set_variable = { name = et_budget_cooldown days = 30 }
        }

        # Bureaucracy modifier logic always runs
        if = {
            limit = {
                bureaucracy < -10
                NOT = { has_modifier = et_negative_bureaucracy }
            }
            add_modifier = { name = et_negative_bureaucracy days = 365 hidden = yes }
        }
    }
}
