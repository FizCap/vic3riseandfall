# Reusable effects for dynamic country candidate marking

# Set cede marker on current state for its owner
et_mark_state_for_dyn_country_effect = {
    set_variable = dynamic_state_cede
}

# Clear cede marker on current state for its owner
et_clear_state_dyn_country_effect = {
    remove_variable = dynamic_state_cede
}
